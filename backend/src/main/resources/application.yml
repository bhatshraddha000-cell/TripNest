spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tripnest_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: tripnest_user
    password: TripNest@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect   
        format_sql: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: dummy  # you'll replace later
            client-secret: dummy
            scope: email, profile

  jwt:
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970   # 256-bit key (for demo)
    expiration: 86400000   # 1 day in milliseconds

server:
  port: 8080